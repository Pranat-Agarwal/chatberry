name: Deploy to Render

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger Render Deploy
        run: |
          curl -X POST ${{'https://api.render.com/deploy/srv-d74rpghr0fns73d3coa0?key=UeIykqofwhE'}}